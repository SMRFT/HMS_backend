version: '3.8'

services:
  backend:
    build: .
    container_name: hms-django
    ports:
      - "2609:2609"
    volumes:
      - .:/app
    environment:
      - DEBUG=1
    extra_hosts:
      - "host.docker.internal:host-gateway"
